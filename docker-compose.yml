version: '3.8'

services:
  app:
    image: my-cicd-app:latest
    container_name: my-cicd-app-dev
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    restart: always